# Defaults for gracie initscript
# sourced by /etc/init.d/gracie
# installed at /etc/default/gracie by the maintainer scripts

# Host (name or IP address) on which the daemon will listen.
#HOST=localhost

# TCP port number on which the daemon will listen.
#PORT=8000
